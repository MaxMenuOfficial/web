<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # 🧱 Archivos físicos primero
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]


  # ✅ HTML versión limpia del widget: /widget/{id}
  RewriteRule ^widget/([A-Za-z0-9_-]+)$ widget.php?id=$1 [QSA,L]

  # ✅ JS loader versionado 1 año: /js/{restaurantId}
  RewriteRule ^js/([A-Za-z0-9_-]+)$ js/widget.js?id=$1 [QSA,L]

  # ✅ Página pública: /{id}
  RewriteRule ^([A-Za-z0-9_-]+)$ menu.php?id=$1 [QSA,L]
  
</IfModule>