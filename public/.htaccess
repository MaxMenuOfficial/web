RewriteEngine On

# Permitir usar URLs sin .php
# /pricing        → /pricing.php
# /about/contact  → /about/contact.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.+?)/?$ $1.php [L]

