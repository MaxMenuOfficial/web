steps:
  # 🛠️ 1. Construir imagen a partir del Dockerfile (usa la imagen base ya construida)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t',
        'europe-west1-docker.pkg.dev/maxmenu-447510/menu-service/menu-service',
        '.'
      ]

  # 🚀 2. Subir imagen a Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west1-docker.pkg.dev/maxmenu-447510/menu-service/menu-service']

images:
  - 'europe-west1-docker.pkg.dev/maxmenu-447510/menu-service/menu-service'